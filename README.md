# Variables
Variables is a WordPress developer plugin that stores site wide variables for a client.

This allows for content that might normally be hard-coded into a custom build, (such as links to social media, phone numbers, and opening hours) to be accessible from WordPress.
